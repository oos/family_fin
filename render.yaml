services:
  # Backend API Service
  - type: web
    name: family-finance-api-v2
    env: python
    plan: starter
    pythonVersion: 3.11
    buildCommand: pip install -r requirements-prod.txt && flask db upgrade
    startCommand: gunicorn --bind 0.0.0.0:$PORT wsgi:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: FLASK_APP
        value: app.py
      - key: FLASK_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: family-finance-db-v2
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: CORS_ORIGINS
        value: https://family-finance-frontend-v2.onrender.com
    healthCheckPath: /api/health

  # Frontend Service
  - type: static
    name: family-finance-frontend-v2
    buildCommand: |
      npm install
      npm run build
    staticPublishPath: ./build
    envVars:
      - key: REACT_APP_API_URL
        value: https://family-finance-api-v2.onrender.com

databases:
  - name: family-finance-db-v2
    plan: free
